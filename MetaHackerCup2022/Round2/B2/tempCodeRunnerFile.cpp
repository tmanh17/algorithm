for (int j = 0; j < N; j++) {
            //     ans = ans + (x[i] - a[j]) * (x[i] - a[j]) % MOD;
            //     ans = ans % MOD;
            //     ans = ans + (y[i] - b[j]) * (y[i] - b[j]) % MOD;
            //     ans = ans % MOD;
            // }